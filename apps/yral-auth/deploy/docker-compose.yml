services:

  yral-auth:
    image: ${APP_IMAGE:-ghcr.io/dolr-ai/yral-auth}:${IMAGE_TAG:-latest}
    restart: unless-stopped
    environment:
      # Secrets (injected at runtime)
      COOKIE_KEY: ${COOKIE_KEY}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      JWT_EC_PEM: ${JWT_EC_PEM}
      CLIENT_JWT_ED_PEM: ${CLIENT_JWT_ED_PEM}
      APPLE_AUTH_KEY_PEM: ${APPLE_AUTH_KEY_PEM}
      WHATSAPP_API_KEY: ${WHATSAPP_API_KEY}
      # Public environment variables (non-sensitive)
      PORT: "8080"
      GOOGLE_CLIENT_ID: 804814798298-taifmq3k6olk9bjr1cqb9gfnp1mssqqb.apps.googleusercontent.com
      APPLE_CLIENT_ID: com.yral.yral-auth
      APPLE_TEAM_ID: 2UL556KNXC
      APPLE_KEY_ID: 9DWVYWDYD8
      JWT_PUB_EC_PEM: |
        -----BEGIN PUBLIC KEY-----
        MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcDQgAEoqN3/0RNfrnrnYGxKBgy/qHnmITr
        +6ucjxStx7tjA30QJZlWzo0atxmY8y9dUR+eKQI0SnbQds4xLEU8+JGm8Q==
        -----END PUBLIC KEY-----
      CLIENT_JWT_PUB_ED_PEM: |
        -----BEGIN PUBLIC KEY-----
        MCowBQYDK2VwAyEAKpZNfPOONR9AXiaGmHv8AHemm4SNK0uh+mf523ho5KU=
        -----END PUBLIC KEY-----
      # Leptos configuration
      LEPTOS_OUTPUT_NAME: yral-auth
      LEPTOS_SITE_ROOT: site
      LEPTOS_SITE_PKG_DIR: pkg
      LEPTOS_SITE_ADDR: 0.0.0.0:8080
      LEPTOS_ENV: PROD
      # New common dragonfly setup for redis store
      DRAGONFLY_REDIS_STORE_PASSWORD: ${DRAGONFLY_REDIS_STORE_PASSWORD}
      DRAGONFLY_REDIS_STORE_HOSTS: ${DRAGONFLY_REDIS_STORE_HOSTS}
      DRAGONFLY_REDIS_STORE_CA_CERT: ${DRAGONFLY_REDIS_STORE_CA_CERT}
      DRAGONFLY_REDIS_STORE_CLIENT_CERT: ${DRAGONFLY_REDIS_STORE_CLIENT_CERT}
      DRAGONFLY_REDIS_STORE_CLIENT_KEY: ${DRAGONFLY_REDIS_STORE_CLIENT_KEY}
      
    networks:
      - yral-net

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - yral-net
    depends_on:
      - yral-auth

networks:
  yral-net:
    driver: bridge

volumes:
  caddy_data:
  caddy_config:
