# Use shell `source` instead of `dotenv_if_exists` because direnv's dotenv
# parser chokes on multi-line PEM values. Shell `source` handles them fine,
# and dotenvy (used by the Rust app) also supports multi-line quoted values.
if [ -f .env ]; then
    set -a
    source .env
    set +a
fi
