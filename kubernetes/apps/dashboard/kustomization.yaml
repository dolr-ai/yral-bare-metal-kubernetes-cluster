apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: dashboard-html
    namespace: dashboard
    files:
      - index.html
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
