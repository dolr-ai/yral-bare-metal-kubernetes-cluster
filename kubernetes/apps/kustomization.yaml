apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Add app subdirectories here as they are onboarded.
resources:
  - dashboard
  - yral-auth
  - yral-web
  - yral-metadata
  - off-chain-agent
  - my-website
