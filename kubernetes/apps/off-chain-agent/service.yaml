apiVersion: v1
kind: Service
metadata:
  name: off-chain-agent
  namespace: off-chain-agent
spec:
  selector:
    app: off-chain-agent
  ports:
    - port: 50051
      targetPort: 50051
      name: http