apiVersion: v1
kind: Service
metadata:
  name: yral-legacy
  namespace: yral-legacy
spec:
  type: ClusterIP
  selector:
    app: yral-legacy
  ports:
    - name: http
      port: 8080
      targetPort: 8080