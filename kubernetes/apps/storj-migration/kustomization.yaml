apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# One-shot Storj migration job — NOT added to the parent apps/kustomization.yaml.
# Applied and torn down manually; not managed by Flux.
resources:
  - namespace.yaml
  - secret.sops.yaml
  - job.yaml
