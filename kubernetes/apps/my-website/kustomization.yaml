apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# my-website — SvelteKit static site served by nginx.
# Harbor project created — build should succeed now.
resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - reference-grant.yaml
  - build-reference-grant.yaml
  - harbor-pull-secret.sops.yaml
  - github-clone-secret.sops.yaml
  - build.yaml
  - image-repository.yaml
  - image-policy.yaml
  - image-update-automation.yaml
  - tekton-trigger.yaml
  - certificate.yaml