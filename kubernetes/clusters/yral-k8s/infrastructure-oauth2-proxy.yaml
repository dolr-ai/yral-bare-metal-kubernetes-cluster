apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-oauth2-proxy
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/infrastructure/oauth2-proxy
  prune: true
  wait: true
  timeout: 2m
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age
