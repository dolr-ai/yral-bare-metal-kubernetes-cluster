apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-longhorn-backup
  namespace: flux-system
spec:
  interval: 1h
  path: ./kubernetes/infrastructure/longhorn-backup
  prune: true
  wait: true
  timeout: 5m
  dependsOn:
    - name: infrastructure-longhorn
  sourceRef:
    kind: GitRepository
    name: flux-system
  decryption:
    provider: sops
    secretRef:
      name: sops-age