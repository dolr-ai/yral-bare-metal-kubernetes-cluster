# Tekton EventListener HTTPRoute for yral-metadata build triggers
#
# Exposes the Tekton EventListener at:
#   https://yral-metadata-build.yral.com/
#
# GitHub push webhooks are sent to this endpoint. The EventListener creates
# a BuildRun via Shipwright/BuildKit.
#
# TLS: covered by existing wildcard *.yral.com cert (cert-manager, letsencrypt-prod).
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: yral-metadata-build-redirect
  namespace: kube-system
spec:
  parentRefs:
    - name: web-gateway
      namespace: kube-system
      sectionName: http
  hostnames:
    - yral-metadata-build.yral.com
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: yral-metadata-build
  namespace: kube-system
spec:
  parentRefs:
    - name: web-gateway
      namespace: kube-system
      sectionName: https
  hostnames:
    - yral-metadata-build.yral.com
  rules:
    - backendRefs:
        - name: el-yral-metadata-build-listener
          namespace: yral-metadata
          port: 8080