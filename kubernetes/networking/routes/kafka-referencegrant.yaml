apiVersion: gateway.networking.k8s.io/v1beta1
kind: ReferenceGrant
metadata:
  name: kafka-gateway-access
  namespace: kafka
spec:
  from:
    - group: gateway.networking.k8s.io
      kind: TLSRoute
      namespace: kube-system
  to:
    - group: ""
      kind: Service
