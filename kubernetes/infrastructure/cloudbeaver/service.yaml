apiVersion: v1
kind: Service
metadata:
  name: cloudbeaver
  namespace: cloudbeaver
spec:
  selector:
    app: cloudbeaver
  ports:
    - port: 8978
      targetPort: 8978
      protocol: TCP
      name: http
