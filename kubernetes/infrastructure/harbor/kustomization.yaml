apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: harbor
namespace: harbor

resources:
  - namespace.yaml
  - reference-grant.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - harbor-admin-secret.sops.yaml
  - harbor-db-superuser.sops.yaml
  # cnpg-cluster.yaml temporarily removed to recreate the Harbor DB from scratch.
  # Re-add after Flux has pruned the old Cluster + PVCs.
