# Tekton Pipelines — execution engine for Shipwright builds.
# Pinned to v1.12.2 LTS (Shipwright v0.20.3 supports v1.3/v1.6/v1.9/v1.12 — NOT v1.14+).
# Raw upstream release YAML, committed to repo (gotk-components.yaml style).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - release.yaml