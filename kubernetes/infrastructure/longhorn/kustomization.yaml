apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Longhorn operator + encrypted StorageClasses. The SCs are enabled now that
# worker-1 is being reprovisioned as the first Longhorn node.
resources:
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
  - crypto-secret.sops.yaml
  - storageclass-2replica.yaml
  - storageclass-1replica.yaml
