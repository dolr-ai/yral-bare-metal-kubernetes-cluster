apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
  namespace: longhorn-system
spec:
  interval: 1h
  chart:
    spec:
      chart: longhorn
      version: "1.11.1"
      sourceRef:
        kind: HelmRepository
        name: longhorn
        namespace: longhorn-system
  timeout: 10m
  upgrade:
    remediation:
      retries: 3
  values:
    defaultSettings:
      # 2 replicas by default — all volumes get a second copy for automatic failover
      # on node loss. Workloads with built-in app-level replication (e.g. Kafka:
      # 3 brokers, replication.factor=3) explicitly opt down to 1 replica via the
      # longhorn-1replica StorageClass to avoid doubling write traffic unnecessarily.
      defaultReplicaCount: 2

      # Reserve 5% of each disk for the OS, kubelet, and containerd image cache.
      # Kubelet's default imageGCHighThresholdPercent (85%) automatically evicts
      # unused images before the disk fills — 5% (~48 Gi on 954 Gi disks) is
      # sufficient headroom alongside automatic GC. Default was 30% (~286 Gi).
      storageReservedPercentageForDefaultDisk: 5

      # Minimum physical free space that must remain after a volume expansion.
      # Set to 0 because storageReservedPercentageForDefaultDisk (5%) already
      # protects the host — this second guard would otherwise block expansions
      # even when plenty of schedulable space exists. Default was 25%.
      storageMinimalAvailablePercentage: 0

      # Prevent cross-zone replica spreading. The default (true) pushes replicas to
      # different topology zones — with HEL1+FSN1 workers that means one replica in
      # Helsinki and one in Falkenstein, requiring every write to cross WAN synchronously.
      # With csiAllowedTopologyKeys enforcing same-region placement and
      # dataLocality: best-effort pinning one replica to the pod's node, this setting
      # prevents secondary replicas from drifting to a different zone within the region.
      replicaZoneSoftAntiAffinity: false
      # NOTE: Longhorn 1.11.1 has no replica-region-soft-anti-affinity setting.
      # Same-region enforcement uses csiAllowedTopologyKeys + allowedTopologies in
      # StorageClasses + WaitForFirstConsumer (two-layer mechanism).
      # Cross-region replicas on pre-existing volumes must be corrected by patching
      # spec.nodeSelector on the Volume CR — Longhorn's reconciliation loop then
      # continuously enforces the constraint.

      # Region-constrained REPLICA PLACEMENT — two-layer mechanism.
      #
      # Layer 1: csiAllowedTopologyKeys (this setting)
      #   Causes Longhorn CSI node plugins to *advertise* topology.kubernetes.io/region
      #   to the k8s external-provisioner when registering. The external-provisioner
      #   then passes the pod's region into CreateVolume's accessibilityRequirements,
      #   and Longhorn's internal scheduler uses it to confine replica placement.
      #
      #   Limitation: advertisement requires the Longhorn CSI plugin pods to have
      #   restarted *after* this setting was applied. PVCs provisioned during the
      #   re-registration window get accessibilityRequirements: null and replicas are
      #   placed freely. Confirmed by the geoip-db volume (created 2026-03-26 with
      #   null accessibilityRequirements despite this setting being active since
      #   2026-03-23 — CSI pods hadn't yet re-registered).
      #
      # Layer 2: allowedTopologies on the StorageClasses (see persistence section)
      #   Provisioner-side guarantee: the external-provisioner always evaluates
      #   allowedTopologies regardless of CSI pod registration state. It intersects
      #   allowedTopologies with the selected node's labels (WaitForFirstConsumer) to
      #   generate requisite topology, closing the re-registration race window.
      #   Both StorageClasses (longhorn and longhorn-1replica) carry this field.
      #
      # Together these two layers ensure all new PVCs get accessibilityRequirements
      # set to the provisioning pod's region, and Longhorn enforces it continuously.
      #
      # IMPORTANT: For Longhorn (network-attached storage), this setting controls
      # where Longhorn places *replicas*, NOT where pods are scheduled. Longhorn is
      # accessible from any node, so its CSI driver reports every node as an
      # accessible topology endpoint. The resulting PV nodeAffinity is OR'd across
      # all nodes/regions — permissive, not a pod scheduling constraint.
      #
      # Pod scheduling constraint for existing stateful PVs:
      #   The 7 stateful PVs (kafka-0/1/2, loki, prometheus, clickhouse, metabase)
      #   have been manually patched with a single-term region nodeAffinity. This
      #   IS a real pod scheduling constraint (single OR term = required). New PVCs
      #   do not get this automatically and must be patched if required.
      csiAllowedTopologyKeys: "topology.kubernetes.io/region"

      # Enable the revision counter globally. This controls the --disableRevCounter
      # flag on every engine process. With counter enabled (i.e. disableRevisionCounter
      # false), a rejoining replica only syncs the delta written while it was down —
      # not the full volume. The counter tracks which blocks changed; without it,
      # Longhorn forces a full rebuild of all data every time a replica's instance-
      # manager restarts (e.g. on every node reboot or upgrade), which on 250 Gi
      # volumes is a 250 Gi network transfer that accomplishes nothing.
      # NOTE: Must use the JSON per-engine-type format (Longhorn v1.11+). The string
      # "false" is a truthy non-empty value in Go templates → the chart would render
      # {"v1":"true"} (counter disabled). Only the explicit JSON form produces the
      # correct ConfigMap entry that the Longhorn Setting controller reads.
      # This is the defaultSettings key — it sets the global Setting CR
      # (disable-revision-counter) which controls engine process flags. The matching
      # persistence.disableRevisionCounter below sets the same value on the StorageClass
      # for new volume creation defaults.
      disableRevisionCounter: '{"v1":"false"}'

      # Allow Longhorn to rebalance replica placement over time. best-effort tries to
      # spread replicas evenly across nodes and zones, and evicts replicas from disks
      # under storage pressure. This is required for Longhorn to remove excess replicas
      # when spec.numberOfReplicas is reduced on an attached volume — without it,
      # cleanupAutoBalancedReplicas returns immediately and extra replicas are never
      # removed. With 35 workers, 13 zones, and replicaZoneSoftAntiAffinity: false,
      # best-effort is largely a no-op for healthy volumes: new replicas are already
      # spread by the scheduler; rebalancing only triggers when imbalance or disk
      # pressure is detected. This setting is also the prerequisite for the
      # replicaAutoBalance: best-effort pass planned during the 1.11.x upgrade.
      replicaAutoBalance: best-effort

      # How long (minutes) a replica can be offline before Longhorn marks it stale
      # and sets failedAt, triggering a full rebuild on rejoin. The default (30 min)
      # fires during normal rolling upgrades on nodes that host Kafka brokers — the
      # Kafka PDB (minAvailable: 2) holds the pre-drain wait until the cluster is
      # healthy, and drain + eviction has exceeded 30 min on multiple workers.
      # 90 min is 2× our worst observed drain time, still aggressive enough to
      # distinguish scheduled maintenance from genuine node failures.
      staleReplicaTimeout: 90

      # S3 backup target on Hetzner Object Storage.
      # A /longhorn prefix separates these backups from Velero's in the same bucket.
      # Credential secret keys: AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_ENDPOINTS,
      # VIRTUAL_HOSTED_STYLE (false = path-style addressing required by Hetzner).
      backupTarget: "s3://yral-bare-metal-kubernetes-cluster-control-plane-backup@hel1/longhorn"
      backupTargetCredentialSecret: "longhorn-s3-backup-credentials"

    persistence:
      # Must match defaultSettings.defaultReplicaCount — controls the numberOfReplicas
      # parameter on the default Longhorn StorageClass, which is what Kubernetes uses
      # when provisioning PVCs. If only defaultSettings is set, the StorageClass retains
      # the Helm chart default of 3.
      defaultClassReplicaCount: 2

      # One replica on the pod's node (local I/O, no network hop). best-effort means
      # Longhorn migrates a local replica to the pod's node in the background after
      # scheduling. If the pod moves to a new node within the region, I/O temporarily
      # uses the intra-region path until the local rebuild completes (fast — Hetzner
      # intranet is ~10 Gbps). This is the natural same-node preference mechanism:
      # the scheduler tends to return pods to the same node (resource stability), and
      # the local replica being there reinforces that tendency.
      defaultDataLocality: best-effort

      # Defer PV binding and provisioning until the pod is scheduled. The CSI provisioner
      # then uses the pod's node topology (region, via csiAllowedTopologyKeys) to
      # constrain the PV nodeAffinity to the pod's region. Without WaitForFirstConsumer,
      # the PV is provisioned immediately with no node context, so csiAllowedTopologyKeys
      # has nothing to infer from and region constraint is not applied.
      volumeBindingMode: "WaitForFirstConsumer"

      # allowedTopologies is the second layer of same-region enforcement alongside
      # csiAllowedTopologyKeys. The external-provisioner evaluates this field
      # regardless of CSI pod registration state — closing the race window where a
      # PVC provisioned just after a Longhorn CSI pod restart would get
      # accessibilityRequirements: null and land replicas cross-region.
      # Both values (falkenstein, helsinki) are listed so any region is valid;
      # WaitForFirstConsumer then narrows to exactly the pod's region at bind time.
      allowedTopologies:
        - matchLabelExpressions:
            - key: topology.kubernetes.io/region
              values:
                - falkenstein
                - helsinki

      # Enable the revision counter so Longhorn can determine which blocks changed
      # while a replica was offline (e.g. during a node reboot). With this enabled,
      # a rejoining replica only syncs the delta written while it was down — not the
      # full volume. With disableRevisionCounter: "true" (the Longhorn chart default),
      # Longhorn cannot distinguish dirty from clean blocks, so it forces a full
      # rebuild of the entire volume every time a replica's instance-manager restarts
      # (i.e. on every node reboot). On 250 Gi volumes that is a 250 Gi network
      # transfer that accomplishes nothing — the data was intact on disk.
      # The cost of enabling the counter is one extra fsync per write per replica for
      # the revision.counter file — negligible on NVMe.
      # NOTE: Must use the JSON per-engine-type format (Longhorn v1.11+). Using the
      # plain string "false" is a truthy non-empty string in Go templates → the chart
      # renders {"v1":"true"} instead. Only boolean false or the explicit JSON form
      # '{"v1":"false"}' produces the correct ConfigMap entry.
      disableRevisionCounter: '{"v1":"false"}'
