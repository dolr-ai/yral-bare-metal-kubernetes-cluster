apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth2-proxy-harbor
  namespace: oauth2-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: oauth2-proxy-harbor
  template:
    metadata:
      labels:
        app: oauth2-proxy-harbor
    spec:
      containers:
        - name: oauth2-proxy
          image: quay.io/oauth2-proxy/oauth2-proxy:v7.15.3
          args:
            - --provider=google
            - --email-domain=gobazzinga.io
            - --google-group=developer-admin@gobazzinga.io
            - --google-admin-email=s@gobazzinga.io
            - --google-service-account-json=/etc/oauth2-proxy/service-account.json
            - --upstream=http://harbor.harbor.svc.cluster.local
            - --http-address=0.0.0.0:4180
            - --redirect-url=https://harbor.yral.com/oauth2/callback
            - --cookie-name=_oauth2_proxy_harbor
            - --cookie-secure=true
            - --cookie-expire=720h
            - --skip-provider-button=true
          env:
            - name: OAUTH2_PROXY_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-secrets
                  key: client-id
            - name: OAUTH2_PROXY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-secrets
                  key: client-secret
            - name: OAUTH2_PROXY_COOKIE_SECRET
              valueFrom:
                secretKeyRef:
                  name: oauth2-proxy-secrets
                  key: cookie-secret
          volumeMounts:
            - name: service-account
              mountPath: /etc/oauth2-proxy
              readOnly: true
          ports:
            - containerPort: 4180
              name: http
          livenessProbe:
            httpGet:
              path: /ping
              port: 4180
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 64Mi
      volumes:
        - name: service-account
          secret:
            secretName: oauth2-proxy-secrets
            items:
              - key: service-account-json
                path: service-account.json
