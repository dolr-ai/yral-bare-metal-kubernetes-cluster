apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy-trino
  namespace: oauth2-proxy
spec:
  selector:
    app: oauth2-proxy-trino
  ports:
    - port: 4180
      targetPort: 4180
      name: http
