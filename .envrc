# Activate the project Python venv (provides ansible-vault, ansible-playbook, etc.)
source ./.venv/bin/activate

# Load secrets from .env (generated by scripts/setup-local-env.sh, gitignored)
dotenv_if_exists
