---
# Drain Node for Maintenance
# Usage: ansible-playbook operations/drain-node.yml -e target_host=worker-X

- name: Drain node for maintenance
  hosts: localhost
  gather_facts: false

  roles:
    - role: ../roles/node-drain
