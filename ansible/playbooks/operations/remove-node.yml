---
# Remove Node from Kubernetes Cluster
# Usage: ansible-playbook operations/remove-node.yml -e target_host=worker-X

- name: Remove node from cluster
  hosts: localhost
  gather_facts: false

  roles:
    - role: longhorn-evict # Drain all Longhorn replicas before removing the node
    - role: node-remove
