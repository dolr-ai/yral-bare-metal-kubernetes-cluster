# Root mise configuration — loads .env (secrets, gitignored) and activates Python venv.
# Generated by scripts/setup-local-env.sh. Re-run after vault changes.
[env]
_.file = '.env'
_.path = '.venv/bin'